language: python
python:
  - "2.7"
# command to install dependencies
install: "python setup.py install"
# command to run tests
script: . travis.sh
branches:
  only:
    - master
    - v1-dev
    - v1-dev-aaron
